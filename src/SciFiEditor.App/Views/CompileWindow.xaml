<Window x:Class="SciFiEditor.App.Views.CompileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:SciFiEditor.App.Resources"
        Title="{x:Static res:Strings.CompileWindowTitle}"
        Height="220" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static res:Strings.CompileFormatLabel}" Margin="0,0,0,8" />
        <ComboBox Grid.Row="1" ItemsSource="{Binding FormatOptions}" DisplayMemberPath="DisplayName"
                  SelectedValuePath="Value" SelectedValue="{Binding SelectedFormat}" Margin="0,0,0,16" />

        <Button Grid.Row="2" Content="{x:Static res:Strings.CompileExportButton}" Command="{Binding ExportCommand}"
                HorizontalAlignment="Left" Width="160" Margin="0,0,0,8" />

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="DarkRed" />
    </Grid>
</Window>
