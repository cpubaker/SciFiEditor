<Window x:Class="SciFiEditor.App.Views.NewProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:SciFiEditor.App.Resources"
        Title="{x:Static res:Strings.DialogNewProjectTitle}"
        Height="180" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="{x:Static res:Strings.DialogNewProjectName}" Margin="0,0,0,4" />
            <TextBox x:Name="NameTextBox" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="{x:Static res:Strings.DialogNewProjectLocation}" Margin="0,0,0,4" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="LocationTextBox" Grid.Column="0" IsReadOnly="True" />
                <Button x:Name="BrowseButton" Grid.Column="1" Content="{x:Static res:Strings.DialogBrowse}"
                        Margin="6,0,0,0" Click="BrowseButton_Click" />
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="{x:Static res:Strings.DialogOk}" Width="80"
                    IsDefault="True" Click="OkButton_Click" />
            <Button x:Name="CancelButton" Content="{x:Static res:Strings.DialogCancel}" Width="80"
                    Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
