<Window x:Class="SciFiEditor.App.Views.EntityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:SciFiEditor.App.Resources"
        Title="{x:Static res:Strings.EntityWindowTitle}"
        Height="420" Width="360"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppWindowBackgroundBrush}"
        Foreground="{DynamicResource AppWindowForegroundBrush}">
    <TabControl Margin="8">
        <TabItem Header="{x:Static res:Strings.EntityTabCharacters}">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListBox Grid.Row="0" ItemsSource="{Binding Characters}" DisplayMemberPath="Name"
                         SelectedItem="{Binding SelectedCharacter}" Margin="0,0,0,8" />
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding NewCharacterName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                    <Button Grid.Column="1" Content="{x:Static res:Strings.EntityAddButton}" Command="{Binding AddCharacterCommand}" MinWidth="80" Margin="0,0,8,0" />
                    <Button Grid.Column="2" Content="{x:Static res:Strings.EntityDeleteButton}" Command="{Binding DeleteCharacterCommand}" MinWidth="80" />
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Header="{x:Static res:Strings.EntityTabLocations}">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListBox Grid.Row="0" ItemsSource="{Binding Locations}" DisplayMemberPath="Name"
                         SelectedItem="{Binding SelectedLocation}" Margin="0,0,0,8" />
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding NewLocationName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                    <Button Grid.Column="1" Content="{x:Static res:Strings.EntityAddButton}" Command="{Binding AddLocationCommand}" MinWidth="80" Margin="0,0,8,0" />
                    <Button Grid.Column="2" Content="{x:Static res:Strings.EntityDeleteButton}" Command="{Binding DeleteLocationCommand}" MinWidth="80" />
                </Grid>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
