<Window x:Class="SciFiEditor.App.Views.EntityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:SciFiEditor.App.Resources"
        Title="{x:Static res:Strings.EntityWindowTitle}"
        Height="420" Width="360"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppWindowBackgroundBrush}"
        Foreground="{DynamicResource AppWindowForegroundBrush}">
    <TabControl Margin="8">
        <TabItem Header="{x:Static res:Strings.EntityTabCharacters}">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListBox Grid.Row="0" ItemsSource="{Binding Characters}" DisplayMemberPath="Name"
                         SelectedItem="{Binding SelectedCharacter}" Margin="0,0,0,8" />
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBox Text="{Binding NewCharacterName, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,8,0" />
                    <Button Content="{x:Static res:Strings.EntityAddButton}" Command="{Binding AddCharacterCommand}" Width="80" Margin="0,0,8,0" />
                    <Button Content="{x:Static res:Strings.EntityDeleteButton}" Command="{Binding DeleteCharacterCommand}" Width="80" />
                </StackPanel>
            </Grid>
        </TabItem>
        <TabItem Header="{x:Static res:Strings.EntityTabLocations}">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListBox Grid.Row="0" ItemsSource="{Binding Locations}" DisplayMemberPath="Name"
                         SelectedItem="{Binding SelectedLocation}" Margin="0,0,0,8" />
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBox Text="{Binding NewLocationName, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,8,0" />
                    <Button Content="{x:Static res:Strings.EntityAddButton}" Command="{Binding AddLocationCommand}" Width="80" Margin="0,0,8,0" />
                    <Button Content="{x:Static res:Strings.EntityDeleteButton}" Command="{Binding DeleteLocationCommand}" Width="80" />
                </StackPanel>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
